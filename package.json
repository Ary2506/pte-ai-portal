{
  "name": "pte-core-ai",
  "description": "AI-powered PTE Core preparation, practice, mock tests, scoring, and performance analysis platform.",
  "private": true,
  "scripts": {
    "install:all": "npm --prefix server install && npm --prefix client install",
    "dev": "concurrently \"npm --prefix server run dev\" \"npm --prefix client run dev\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
